
/*
#include "Login.h"
#include<sstream>
#include<string.h>
#include<stdio.h>
#include<vector>
#include "User.h"


bool login(string a, string b)
{
	User u1("david", "1234");
	User u2("Andrei", "1234");

	vector<User> users;
	users.push_back(u1);
	users.push_back(u2);

	for(int i=0; i<users.size();i++)
		if (users[i].getPassword() == b && users[i].getUsername() == a)
			return true;
	return false;
}



*/